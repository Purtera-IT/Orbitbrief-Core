"""Composers — Phase 3 scaffolding placeholder.

Composers turn validated/calibrated retrieval+brain output into the
downstream packet shapes (professional_services packets, OrbitBrief
report sections, etc.).

**This package is intentionally empty.** It exists today only so
:mod:`orbitbrief_core.retrieval` can be statically guarded against
direct imports from here (Phase 2 layering rule, enforced by
``import-linter``: see ``.importlinter`` → ``no-retrieval-bypass``).

Add code here in Phase 3 when the world_model + brains layers are
ready. Until then, importing anything beyond this docstring is the
wrong move.
"""
