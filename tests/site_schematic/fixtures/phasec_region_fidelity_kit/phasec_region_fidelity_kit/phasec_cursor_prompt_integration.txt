Integrate the attached Phase C kit into the latest OrbitBrief Core repo as a parser-only change.

SOURCE OF TRUTH
- Use the Phase C kit files as the source of truth for:
  - hard-page manifest
  - perfect gold standard
  - acceptance checklist
  - implementation order
  - parser-only scope

DO NOT
- do not touch OrbitBrief / projection logic
- do not weaken Phase 1B table spine
- do not replace deterministic fallbacks with unconstrained model logic
- do not relax current parser tests

WHAT TO DO
1. Read the Phase C integration plan and hard-page manifest.
2. Implement Phase C inside the existing `site_schematic` lane.
3. Make Phase C explicitly reuse Phase 1B universal tables as region anchors.
4. Add the hard eval + focused tests.
5. Run the full required validation set.
6. Report exactly which hard pages still miss and why.

SUCCESS CONDITION
The parser should hit the Phase C gold bar on the hard pages from the wireless and Southern Post packet families while remaining regression-safe.
