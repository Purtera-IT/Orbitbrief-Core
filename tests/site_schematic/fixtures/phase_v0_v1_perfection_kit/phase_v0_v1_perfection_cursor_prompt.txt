You are working in the latest OrbitBrief Core repo on the `site_schematic` lane.

This is the Phase V0/V1 PERFECTION PASS.

CURRENT STATE

The first Phase V0/V1 pass is already strong.
Now we want to make it packet-level reliable and 10/10 enough to move on to V2.

PRIMARY GOAL

Harden V0 and V1 by adding:
1. modality calibration and honesty guards
2. vector primitive validation
3. primitive graph quality summaries
4. packet-level V0/V1 pass/fail evaluation

DO NOT:
- touch OrbitBrief
- start V2
- implement full raster fallback yet
- weaken parser truth paths
- regress current-pair parser behavior

IMPLEMENT IN THIS ORDER

## 1. Add helper modules
- `modality_calibration.py`
- `primitive_validation.py`
- `packet_v0_v1_quality.py`

## 2. Patch models
Add first-class contracts for:
- calibrated modality decisions
- validated primitive metrics
- packet-level V0/V1 summaries

## 3. Patch core
- calibrate modality decisions after V0 routing
- store ambiguity flags
- store packet-level V0/V1 summaries in diagnostics/model registry

## 4. Patch observations
- validate raw vector primitives
- keep both raw and validated primitive sets
- preserve provenance

## 5. Patch structure graph
- build primitive graph from validated primitives when available
- add quality counts to diagnostics

## 6. Patch eval harness
Upgrade the V0/V1 eval harness to produce:
- packet-level modality fail counts
- packet-level primitive graph fail counts
- packet-level ambiguity summaries
- packet-level quality rows

## 7. Run full 12-packet V0/V1 evaluation
Also run parser regression spot checks:
- `test_gold_pdf_eval.py`
- `test_universal_table_contract.py`
- `test_phasec_region_fidelity.py`

TARGETS

V0:
- modality_honesty_rate = 1.0
- current_pair_hard_page_modality_consistency = 1.0
- holdout_routing_completeness = 1.0
- packet-level modality failures = 0

V1:
- vector_bbox_presence_rate = 1.0
- primitive_provenance_rate = 1.0
- primitive_graph_construction_rate = 1.0
- leader_candidate_presence_on_expected_pages >= 0.95
- dimension_candidate_presence_on_expected_pages >= 0.9
- packet-level primitive graph failures = 0

Preserve:
- production_kpi_regression_count = 0
- contradiction_lane_separation_rate = 1.0
- parser text/table coverage unchanged

OUTPUT I WANT

1. exact files changed
2. what changed in V0 calibration
3. what changed in V1 validation
4. packet-by-packet V0/V1 results
5. pages still weak, if any
6. whether V0/V1 are now 10/10 enough for V2
7. what the next graphics phase should be after this
