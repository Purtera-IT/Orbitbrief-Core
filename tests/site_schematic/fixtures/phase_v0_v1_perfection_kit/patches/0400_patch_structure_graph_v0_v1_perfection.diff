--- a/src/orbitbrief_core/parser/site_schematic/structure_graph.py
+++ b/src/orbitbrief_core/parser/site_schematic/structure_graph.py
@@
 from orbitbrief_core.parser.site_schematic.vector_primitive_graph import build_vector_primitive_graph
@@
     vector_primitives = getattr(page_observation, "vector_primitives", None) if 'page_observation' in locals() else None
+    validated_vector_primitives = getattr(page_observation, "validated_vector_primitives", None) if 'page_observation' in locals() else None
     if vector_primitives:
-        vgraph = build_vector_primitive_graph(vector_primitives, page_index=page_index)
+        vgraph = build_vector_primitive_graph(validated_vector_primitives or vector_primitives, page_index=page_index)
         graph.diagnostics["vector_primitive_count"] = len(vgraph.primitive_ids)
         graph.diagnostics["vector_leader_candidate_count"] = len(vgraph.leader_candidate_ids)
         graph.diagnostics["vector_connector_candidate_count"] = len(vgraph.connector_candidate_ids)
         graph.diagnostics["vector_dimension_candidate_count"] = len(vgraph.dimension_candidate_ids)
+        graph.diagnostics["vector_validated_primitive_count"] = len(validated_vector_primitives or [])
