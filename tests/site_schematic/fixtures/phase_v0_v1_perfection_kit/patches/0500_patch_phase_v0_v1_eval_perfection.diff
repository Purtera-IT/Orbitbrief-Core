--- a/tests/site_schematic/phase_v0_v1_eval.py
+++ b/tests/site_schematic/phase_v0_v1_eval.py
@@
+from orbitbrief_core.parser.site_schematic.packet_v0_v1_quality import summarize_packet_v0_v1
@@
 def run_phase_v0_v1_eval(...):
@@
+    packet_quality_rows = []
+    for packet_id, rows in packet_rows_by_packet.items():
+        modality_rows = rows.get("page_modality", [])
+        primitive_rows = rows.get("primitive_graph", [])
+        summary = summarize_packet_v0_v1(
+            packet_id=packet_id,
+            page_modality_rows=modality_rows,
+            primitive_graph_rows=primitive_rows,
+        )
+        packet_quality_rows.append({
+            "packet_id": summary.packet_id,
+            "page_count": summary.page_count,
+            "modality_counts": summary.modality_counts,
+            "ambiguous_page_count": summary.ambiguous_page_count,
+            "primitive_count": summary.primitive_count,
+            "validated_primitive_count": summary.validated_primitive_count,
+            "leader_candidate_count": summary.leader_candidate_count,
+            "dimension_candidate_count": summary.dimension_candidate_count,
+            "modality_fail": summary.modality_fail,
+            "primitive_graph_fail": summary.primitive_graph_fail,
+        })
+    summary_json["packet_quality_rows"] = packet_quality_rows
+    summary_json["packet_level_modality_failures"] = sum(1 for r in packet_quality_rows if r["modality_fail"])
+    summary_json["packet_level_primitive_graph_failures"] = sum(1 for r in packet_quality_rows if r["primitive_graph_fail"])
