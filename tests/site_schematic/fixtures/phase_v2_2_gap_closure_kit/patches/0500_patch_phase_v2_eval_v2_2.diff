--- a/tests/site_schematic/phase_v2_eval.py
+++ b/tests/site_schematic/phase_v2_eval.py
@@
+from orbitbrief_core.parser.site_schematic.hardpage_requirement_registry import derive_required_hardpage_types
+from orbitbrief_core.parser.site_schematic.grounded_yield_metrics import compute_grounded_yield_metrics
@@
 def run_phase_v2_eval(...):
@@
+    grounded_yield_rates = []
+    hardpage_grounded_yield_rates = []
+    unresolved_ratios = []
+    room_assoc_hits = 0
+    room_assoc_total = 0
+    connector_quality_hits = 0
+    connector_quality_total = 0
+    family_coverage_rates = []
+    hardpage_rates = []
@@
         hardpage_summary = build_packet_hardpage_summary(packet_id, page_rows)
+        hardpage_summary.required_page_types = derive_required_hardpage_types(page_rows)
         hardpage_rates.append(hardpage_summary.rate)
         packet_row["hardpage_rate"] = hardpage_summary.rate
         packet_row["required_page_types"] = hardpage_summary.required_page_types
         packet_row["satisfied_page_types"] = hardpage_summary.satisfied_page_types
+
+        total_candidates = int(packet_row.get("candidate_symbol_count", 0))
+        grounded_symbols = int(packet_row.get("grounded_symbol_count", 0))
+        unresolved_symbols = int(packet_row.get("unresolved_symbol_count", 0))
+        hardpage_candidates = int(packet_row.get("hardpage_candidate_symbol_count", grounded_symbols))
+        hardpage_grounded = int(packet_row.get("hardpage_grounded_symbol_count", grounded_symbols))
+        expected_family_total = int(packet_row.get("expected_family_total", len(packet_row.get("expected_symbol_families", []))))
+        expected_family_grounded = int(packet_row.get("expected_family_grounded", grounded_symbols))
+        metrics = compute_grounded_yield_metrics(
+            total_candidates=total_candidates,
+            grounded_symbols=grounded_symbols,
+            unresolved_symbols=unresolved_symbols,
+            hardpage_candidates=hardpage_candidates,
+            hardpage_grounded=hardpage_grounded,
+            expected_family_total=expected_family_total,
+            expected_family_grounded=expected_family_grounded,
+        )
+        packet_row["grounded_symbol_yield_rate"] = metrics.grounded_symbol_yield_rate
+        packet_row["hardpage_grounded_symbol_yield_rate"] = metrics.hardpage_grounded_symbol_yield_rate
+        packet_row["unresolved_symbol_ratio"] = metrics.unresolved_symbol_ratio
+        packet_row["expected_family_grounded_coverage_rate"] = metrics.expected_family_grounded_coverage_rate
+        grounded_yield_rates.append(metrics.grounded_symbol_yield_rate)
+        hardpage_grounded_yield_rates.append(metrics.hardpage_grounded_symbol_yield_rate)
+        unresolved_ratios.append(metrics.unresolved_symbol_ratio)
+        family_coverage_rates.append(metrics.expected_family_grounded_coverage_rate)
+
+        for gs in packet_row.get("grounded_symbol_rows", []):
+            room_assoc_total += 1
+            if gs.get("room_device_association_ok", False):
+                room_assoc_hits += 1
+            if gs.get("connector_required", False):
+                connector_quality_total += 1
+                if gs.get("connector_grounding_ok", False):
+                    connector_quality_hits += 1
@@
+    summary["grounded_symbol_yield_rate"] = (sum(grounded_yield_rates) / len(grounded_yield_rates)) if grounded_yield_rates else 1.0
+    summary["hardpage_grounded_symbol_yield_rate"] = (sum(hardpage_grounded_yield_rates) / len(hardpage_grounded_yield_rates)) if hardpage_grounded_yield_rates else 1.0
+    summary["unresolved_symbol_ratio"] = (sum(unresolved_ratios) / len(unresolved_ratios)) if unresolved_ratios else 0.0
+    summary["room_device_association_rate"] = (room_assoc_hits / room_assoc_total) if room_assoc_total else 1.0
+    summary["connector_grounding_quality_rate"] = (connector_quality_hits / connector_quality_total) if connector_quality_total else 1.0
+    summary["expected_family_grounded_coverage_rate"] = (sum(family_coverage_rates) / len(family_coverage_rates)) if family_coverage_rates else 1.0
+    summary["packet_hardpage_semantics_rate"] = (sum(hardpage_rates) / len(hardpage_rates)) if hardpage_rates else 1.0
