--- a/src/orbitbrief_core/parser/site_schematic/semantic_mapper.py
+++ b/src/orbitbrief_core/parser/site_schematic/semantic_mapper.py
@@
+from orbitbrief_core.parser.site_schematic.room_device_association_refinement import score_room_device_association
@@
 def map_candidate_to_semantics(...):
@@
+    room_assoc = score_room_device_association(
+        symbol_bbox=symbol_bbox if 'symbol_bbox' in locals() else None,
+        room_label_bboxes=room_label_bboxes if 'room_label_bboxes' in locals() else [],
+        same_region=bool(same_region if 'same_region' in locals() else False),
+        same_subregion=bool(same_subregion if 'same_subregion' in locals() else False),
+        same_pseudo_page=bool(same_pseudo_page if 'same_pseudo_page' in locals() else False),
+        same_detail_frame=bool(same_detail_frame if 'same_detail_frame' in locals() else False),
+        leader_attached=bool(leader_attached if 'leader_attached' in locals() else False),
+    )
+    mapping_metadata["room_device_association_score"] = room_assoc.score
+    mapping_metadata["room_device_association_ok"] = room_assoc.associated
+    mapping_metadata["room_device_association_reasons"] = room_assoc.reasons
