--- a/src/orbitbrief_core/parser/site_schematic/core.py
+++ b/src/orbitbrief_core/parser/site_schematic/core.py
@@
 from orbitbrief_core.parser.site_schematic.reasoning import build_bounded_graph_reasoning, build_reasoning_summaries
+from orbitbrief_core.parser.site_schematic.page_modality_router import classify_page_modality
@@
 def build_site_schematic_bundle_from_router_input(
@@
     page_observations, observation_diagnostics = build_site_schematic_page_observations(
         router_input=router_input,
         page_texts=page_texts,
         model_registry=model_registry,
         sheet_types=initial_sheet_types,
     )
+    # Phase V0 starter: classify page modality from available diagnostics
+    phase_v0_modality = []
+    for idx, stype in enumerate(initial_sheet_types):
+        obs = page_observations[idx] if idx < len(page_observations) else None
+        vector_path_count = 0
+        image_count = 0
+        table_count = 0
+        line_art_density = 0.0
+        if obs is not None:
+            vector_path_count = int(getattr(obs, "vector_path_count", 0) or 0)
+            image_count = int(getattr(obs, "image_count", 0) or 0)
+            table_count = len(getattr(obs, "tables", []) or [])
+            line_art_density = float(getattr(obs, "line_art_density", 0.0) or 0.0)
+        decision = classify_page_modality(
+            page_index=idx,
+            sheet_type=stype,
+            page_text=page_texts[idx] if idx < len(page_texts) else "",
+            vector_path_count=vector_path_count,
+            image_count=image_count,
+            line_art_density=line_art_density,
+            table_count=table_count,
+        )
+        phase_v0_modality.append(decision)
+    model_registry.setdefault("phase_v0_v1", {})["page_modality"] = [
+        {
+            "page_index": d.page_index,
+            "sheet_type": d.sheet_type,
+            "modality": d.modality,
+            "confidence": d.confidence,
+            "scores": d.scores,
+            "reasons": d.reasons,
+            "diagnostics": d.diagnostics,
+        }
+        for d in phase_v0_modality
+    ]
