--- a/src/orbitbrief_core/parser/site_schematic/extractors/notes_spec_extractor.py
+++ b/src/orbitbrief_core/parser/site_schematic/extractors/notes_spec_extractor.py
@@
 def extract_notes_spec(...):
@@
+    # Residual text-perfection backfill:
+    # if no structured note clauses were emitted but promoted note candidates exist in diagnostics/registry,
+    # convert them into deterministic note clause objects before returning.
+    promoted_note_candidates = []
+    try:
+        promoted_note_candidates = model_registry.get("text_perfection", {}).get("promoted_note_candidates", {}).get(page_index, [])
+    except Exception:
+        promoted_note_candidates = []
+    if not note_clauses and promoted_note_candidates:
+        for cand in promoted_note_candidates:
+            note_clauses.append(
+                build_note_clause_from_text(
+                    cand["text"],
+                    bbox=cand.get("bbox"),
+                    source_mode=cand.get("source_mode", "deterministic_note_promoter_v1"),
+                )
+            )
