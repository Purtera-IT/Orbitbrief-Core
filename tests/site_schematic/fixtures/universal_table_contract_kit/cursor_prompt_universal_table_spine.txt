You are working in the latest OrbitBrief Core repo on the `site_schematic` parser lane.

This is a PHASE-1 PARSER-ONLY IMPLEMENTATION PASS:
UNIVERSAL LOSSLESS TABLE SPINE

IMPORTANT
- Parser only. Do NOT work on OrbitBrief/projection logic.
- Do NOT do broad region-fidelity hardening yet.
- Do NOT rewrite contradiction/reasoning logic.
- This pass is only about lossless table structure and semantic lineage.

GOAL

Implement a universal parser-level table contract and wire semantic extractors to row/cell provenance.

The parser must stop flattening table-like content before semantic extraction.
Every semantic object derived from a table must point back to:
- source_table_id
- source_row_id
- source_cell_ids

USE THESE FILES AS SOURCE OF TRUTH FOR THIS PHASE
(assume I am also giving you these artifacts)

- `universal_table_contract_spec.md`
- `universal_table_contract_schema.json`
- `universal_table_gold_standard.json`

HARD GOLD PAGES TO USE

Wireless packet:
- page 1 / `TC001 TELECOMM SYMBOL LIST`

Southern Post packet:
- page 1 / `T000 PROJECT REQUIREMENTS NOTES & SPECS`
- page 2 / `T001 SYMBOLS & LEGENDS`
- page 3 / `T002 SCHEDULES & MISCELLANEOUS`
- page 12 / `T900 ENLARGED EQUIPMENT ROOM LAYOUTS`
- page 17 / `T905 SECURITY INSTALLATION DETAILS`

MISSION

Implement Phase 1:
1. universal table contract
2. lossless row/cell preservation
3. semantic lineage wiring for table-derived outputs
4. parser-only tests and gold checks on the selected hard pages

WHAT TO IMPLEMENT

1. MODELS / CONTRACT
Add first-class parser models for:
- table
- row
- cell
- semantic lineage references

Required fields are defined in the schema artifact.
Do not invent a weaker schema.

2. OBSERVATION / RECONCILIATION LAYER
Upgrade the parser observation layer so that table-like regions produce stable:
- table objects
- row objects
- cell objects
with bbox + provenance preserved.

If multiple providers contribute candidates (native/Docling/lightweight), keep reconciliation deterministic and auditable.

3. SEMANTIC EXTRACTOR WIRING
Update every table-derived semantic extractor so it reads from preserved rows/cells and emits lineage:
- legend entries
- abbreviation entries
- outlet definitions
- drawing index rows
- schedule rows
- component spec rows
- responsibility matrix rows if applicable

Every semantic output must carry:
- source_table_id
- source_row_id
- source_cell_ids

4. NO SILENT FLATTENING
If the parser cannot confidently preserve a row/cell boundary:
- keep the ambiguity explicit
- fail closed
- do not silently collapse the structure into a blob while pretending it is relational

5. GOLD-STANDARD EVAL
Use the provided gold standard pages and acceptance rules.
I want explicit evaluation for:
- required table kind coverage
- row integrity
- cell integrity
- bbox presence
- lineage completeness
- semantic row/cell reference completeness
- unflagged row/cell merge/split counts

6. TESTS
Add/extend parser-only tests for:
- table object presence
- row/cell bbox + provenance
- semantic lineage references
- page-specific expectations on the hard gold pages
- no regressions to current parser suites

VALIDATION REQUIRED

Run and report:
- existing site_schematic parser tests
- any page-level hard-page table tests you add
- gold table-contract checks for the hard pages
- smoke/gold parser suites that should remain green

ACCEPTANCE CRITERIA

This phase is only done if ALL of these are true:

1. universal table contract exists in code
2. table-like pages emit stable table->row->cell objects
3. semantic outputs reference row/cell provenance
4. hard gold pages are evaluated against the provided gold standard
5. no silent flattening of the selected table pages remains
6. existing parser suites remain green

OUTPUT I WANT

When done, give me:
1. exact files changed
2. exact models/contract added
3. which extractors now use row/cell provenance
4. page-by-page hard gold results
5. remaining structural gaps
6. whether the parser now satisfies the Phase-1 “perfect table spine” standard