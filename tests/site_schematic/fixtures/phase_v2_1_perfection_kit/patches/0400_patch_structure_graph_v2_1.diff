--- a/src/orbitbrief_core/parser/site_schematic/structure_graph.py
+++ b/src/orbitbrief_core/parser/site_schematic/structure_graph.py
@@
 def build_page_structure_graph(...):
@@
+    # V2.1 starter: expose candidate/grounded symbol counts and connector hints in graph diagnostics
+    if 'grounded_symbols' in locals():
+        graph.diagnostics["grounded_symbol_count"] = len(grounded_symbols or [])
+    if 'symbol_candidate_groups' in locals():
+        graph.diagnostics["symbol_candidate_group_count"] = len(symbol_candidate_groups or [])
