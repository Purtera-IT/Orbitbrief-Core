--- a/src/orbitbrief_core/parser/site_schematic/grounding_resolver.py
+++ b/src/orbitbrief_core/parser/site_schematic/grounding_resolver.py
@@
+from orbitbrief_core.parser.site_schematic.grounding_state_policy import choose_grounding_state
+from orbitbrief_core.parser.site_schematic.connector_grounding_refinement import refine_with_connector_context
@@
 def resolve_grounded_symbols(...):
@@
+    # V2.1: connector-aware score refinement + honest state policy
+    refined = refine_with_connector_context(
+        base_score=base_score if 'base_score' in locals() else 0.0,
+        has_connector_candidate=bool(connector_candidate_count if 'connector_candidate_count' in locals() else 0),
+        has_leader_attachment=bool(leader_attachment_count if 'leader_attachment_count' in locals() else 0),
+        riser_context=sheet_type == "riser_diagram" if 'sheet_type' in locals() else False,
+        rack_pathway_context=sheet_type in {"equipment_room_layout", "installation_detail"} if 'sheet_type' in locals() else False,
+    )
+    state = choose_grounding_state(
+        legend_match_score=legend_match_score if 'legend_match_score' in locals() else 0.0,
+        text_association_score=legend_text_score if 'legend_text_score' in locals() else 0.0,
+        connector_score=refined.adjusted_score,
+        room_device_score=room_device_score if 'room_device_score' in locals() else 0.0,
+        page_type_compatibility=page_type_compatibility if 'page_type_compatibility' in locals() else 0.0,
+    )
+    resolution_metadata["grounding_state"] = state.state
+    resolution_metadata["grounding_state_confidence"] = state.confidence
+    resolution_metadata["grounding_state_reasons"] = state.reasons
+    resolution_metadata["connector_refinement_reasons"] = refined.reasons
