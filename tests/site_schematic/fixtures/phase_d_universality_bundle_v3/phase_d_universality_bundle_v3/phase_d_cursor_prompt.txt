You are working in the latest OrbitBrief Core repo on the `site_schematic` lane.

This pass is PHASE D FULL HOLDOUT ACTIVATION + A/B/C/D EXECUTION using the included holdout bundle.

BUNDLE CONTENTS
- current pair PDFs are already present under `pdfs/current_pair/`
- 10 public holdout packet URLs are listed in `holdout_download_manifest.csv`
- fetch helpers exist under `scripts/`
- per-packet gold schemas exist under `gold_packet_schemas/`
- master Phase A-D contract exists in `phase_a_d_gold_schema_master.json`

MISSION
1. Download the 10 holdout PDFs locally using the included fetch script if they are missing.
2. Validate which holdout PDFs are present.
3. Hydrate each per-packet gold schema with real page indices, hard pages, and sheet IDs once the PDF exists.
4. Integrate the holdout packets into a Phase D holdout registry/eval lane.
5. Run the full Phase A-D evaluation across:
   - the current pair (must not regress)
   - every downloaded holdout packet
6. Produce a packet-by-packet and registry-wide summary proving how well the parser generalizes.

DO NOT TOUCH ORBITBRIEF LOGIC.
DO NOT BREAK CURRENT PAIR KPI PATHS.

PHASE A TARGETS
- sheet typing
- observation bbox/provenance
- layout block recall

PHASE B TARGETS
- table kind coverage
- row/cell provenance
- semantic lineage

PHASE C TARGETS
- region fidelity
- locality provenance
- table region reuse
- no overflatten / silent note conflicts

PHASE D TARGETS
- holdout pass rate
- no production KPI regressions
- contradiction lane separation intact
- evidence trace completeness

REQUIRED OUTPUTS
- downloaded/missing holdout report
- hydrated per-packet gold schemas
- packet-by-packet A/B/C/D summaries
- registry-wide Phase D summary
- list of packets that fail and exactly why
- recommendation on what parser component is still blocking universality, if any

SUCCESS CRITERIA
- current pair remains green
- each downloaded holdout packet is honestly scored
- packets not downloaded remain explicitly pending
- no fake universality claims for missing packets
