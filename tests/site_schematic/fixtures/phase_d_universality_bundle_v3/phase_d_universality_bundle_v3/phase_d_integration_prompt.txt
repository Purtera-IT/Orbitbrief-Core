Integrate the included Phase D holdout bundle into the repo.

Steps:
1. Copy the included current pair PDFs into the repo benchmark fixture location if needed.
2. Use `holdout_download_manifest.csv` plus `scripts/fetch_public_holdouts.py` to fetch holdout PDFs locally.
3. Validate presence with `scripts/validate_holdout_presence.py`.
4. Hydrate every packet gold schema under `gold_packet_schemas/` after its PDF is present.
5. Register the hydrated packets in the Phase D holdout lane.
6. Run the full parser suite plus the new Phase A/B/C/D holdout eval.
7. Emit a final report with packet-level and registry-level results.
