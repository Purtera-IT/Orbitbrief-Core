Use this patch kit semantically against the live repo.

1. Read:
- README.md
- merge_checklist.md
- holdout_pass_fail_worksheet.md
- all files in `patches/`

2. Add the new helper modules from `code/src/orbitbrief_core/parser/site_schematic/`.

3. Apply the patch diffs semantically in this order:
- 0100_patch_sheet_type.diff
- 0200_patch_universal_table_spine.diff
- 0300_patch_page_zones.diff
- 0400_patch_core.diff

4. Add the helper test:
- `code/tests/site_schematic/test_holdout_universality_helpers.py`

5. Run:
- current pair canonical eval
- full Phase D holdout universality eval

6. Report:
- exact files changed
- before/after A/B/C/D holdout counts
- before/after current-pair canonical state
- which holdouts changed from fail -> pass
- remaining blockers by holdout packet

Rules:
- keep current pair canonical lanes frozen
- do not touch OrbitBrief logic
- do not touch contradiction truth paths
- if Pass 1 stalls, only then enable bounded observation escalation in `core.py`
