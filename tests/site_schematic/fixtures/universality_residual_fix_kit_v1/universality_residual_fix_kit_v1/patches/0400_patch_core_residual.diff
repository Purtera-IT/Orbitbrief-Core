--- a/src/orbitbrief_core/parser/site_schematic/core.py
+++ b/src/orbitbrief_core/parser/site_schematic/core.py
@@
 from orbitbrief_core.parser.site_schematic.reasoning import build_bounded_graph_reasoning, build_reasoning_summaries
+from orbitbrief_core.parser.site_schematic.region_bbox_completion import (
+    complete_region_bbox_from_children,
+)
@@
 def build_site_schematic_bundle_from_router_input(
@@
     # after decomposition
+    try:
+        for region in page_regions:
+            children = [d for d in detail_regions if getattr(d, "parent_region_id", None) == getattr(region, "region_id", None)]
+            complete_region_bbox_from_children(region, children, table_anchors=getattr(page_observation, "tables", []) if 'page_observation' in locals() else [])
+    except Exception:
+        pass
