--- a/src/orbitbrief_core/parser/site_schematic/zoning/page_zones.py
+++ b/src/orbitbrief_core/parser/site_schematic/zoning/page_zones.py
@@
 import re
 from typing import Iterable
+from orbitbrief_core.parser.site_schematic.region_bbox_completion import (
+    complete_region_bbox_from_children,
+    ensure_locality_provenance,
+)
@@
 def build_page_decomposition(...):
     # existing logic
     ...
+    # Residual pass: conservative bbox / locality completion.
+    try:
+        for region in regions:
+            children = [
+                sr for sr in subregions
+                if getattr(sr, "parent_region_id", None) == getattr(region, "region_id", None)
+            ] if subregions is not None else []
+            tables_here = []
+            if page_observation is not None and getattr(page_observation, "tables", None):
+                tables_here = [t for t in page_observation.tables if getattr(t, "bbox", None) is not None]
+            complete_region_bbox_from_children(region, children, table_anchors=tables_here)
+        for note in scoped_notes if 'scoped_notes' in locals() else []:
+            ensure_locality_provenance(
+                note,
+                parent_region_id=getattr(note, "parent_region_id", ""),
+                detail_region_id=getattr(note, "detail_region_id", ""),
+                subregion_id=getattr(note, "subregion_id", ""),
+                pseudo_page_id=getattr(note, "pseudo_page_id", ""),
+            )
+    except Exception:
+        pass
