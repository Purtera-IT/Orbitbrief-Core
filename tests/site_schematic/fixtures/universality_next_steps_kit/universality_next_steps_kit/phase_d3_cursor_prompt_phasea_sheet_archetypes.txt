You are working in the latest OrbitBrief Core repo on the `site_schematic` lane.

This is the Phase A residual universality pass.

CURRENT STATE
The universality evaluator is aligned enough and the next biggest residual issue after Phase C is sheet archetype drift on unfamiliar title blocks / sheet labels.

PRIMARY GOAL
Improve sheet typing / title block / archetype mapping on holdouts without regressing the current pair.

WHAT TO IMPLEMENT
- strengthen sheet id extraction robustness
- broaden archetype aliasing and fallback normalization
- improve riser vs legend vs schedule vs floorplan disambiguation
- improve title block signal usage when labels drift

FILES MOST LIKELY
- `src/orbitbrief_core/parser/site_schematic/classification/sheet_type.py`
- title/header helpers if present
- universality evaluator only if it needs better archetype-aware reporting

VALIDATION
- full Phase D universality eval
- report before/after Phase A pass count
- keep current-pair canonical scoring unchanged

ACCEPTANCE
- holdout Phase A pass count >= 6/10
- current pair canonical sheet counts remain matching
