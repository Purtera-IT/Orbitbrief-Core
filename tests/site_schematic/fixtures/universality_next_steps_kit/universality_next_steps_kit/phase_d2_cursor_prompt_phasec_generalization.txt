You are working in the latest OrbitBrief Core repo on the `site_schematic` lane.

This is the NEXT UNIVERSALITY FIX PASS and it is Phase C-first.

CURRENT STATE

What is already true:
- the current pair are back on canonical scoring and are protected
- Phase A improved from 2 -> 4 holdout passes
- Phase B improved from 3 -> 5 holdout passes
- Phase C improved from 0 -> 1 holdout passes
- production_kpi_regression_count is now 0
- contradiction-lane separation is intact
- universality is still blocked mainly by Phase C

PRIMARY GOAL

Generalize Phase C parser behavior across holdouts:
- locality provenance
- global-vs-local note separation
- detail-locality references
- multi-column note preservation
- mixed-page locality contracts

DO NOT:
- touch OrbitBrief logic
- rewrite contradiction lane
- rewrite truth paths
- do a giant architecture rewrite
- replace parser with a model

WHAT TO IMPLEMENT

1. Locality-first region contract improvements
- strengthen locality metadata on regions/subregions/pseudo-pages
- add explicit locality confidence and scope class where useful

2. Generalized multi-column detection
- infer columns from layout/text geometry, not just explicit header hits
- support multi-column note/spec pages with unfamiliar layouts

3. Stronger global-vs-local note separation
- use detail-frame overlap, pseudo-page locality, keyed-note cues, and proximity
- keep ambiguous cases fail-closed, not silently global

4. Mixed-page locality generalization
- preserve column-local / detail-local / page-global explicitly
- reduce silent note-scope conflicts

5. Table-aware locality reuse
- if note/schedule/legend blocks are backed by universal tables, preserve locality and provenance through that path

FILES MOST LIKELY TO CHANGE
- src/orbitbrief_core/parser/site_schematic/zoning/page_zones.py
- src/orbitbrief_core/parser/site_schematic/core.py
- src/orbitbrief_core/parser/site_schematic/models.py (only if needed)
- tests/site_schematic/phase_d_universality_eval.py

VALIDATION REQUIRED
Run and report:
- current pair canonical Phase B/C eval
- full Phase D holdout universality eval
- packet-by-packet A/B/C summaries
- before/after Phase C holdout pass count
- before/after silent note-scope conflict counts
- before/after multi-column preservation rate
- before/after locality provenance rate
- before/after detail-locality reference rate

ACCEPTANCE CRITERIA
This pass is only done if:
1. production current-pair Phase C stays perfect
2. holdout Phase C pass count improves materially (target >= 5/10)
3. production_kpi_regression_count remains 0
4. contradiction_lane_separation_rate remains 1.0
5. evidence_trace_completeness_rate does not regress

OUTPUT I WANT
1. exact files changed
2. what changed in locality modeling
3. what changed in multi-column discovery
4. what changed in note scope separation
5. before/after holdout Phase C pass count
6. before/after current-pair canonical Phase C status
7. remaining blockers after this pass
