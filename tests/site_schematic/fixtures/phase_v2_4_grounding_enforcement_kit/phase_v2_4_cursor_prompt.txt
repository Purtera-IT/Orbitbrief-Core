You are working in the latest OrbitBrief Core repo on the `site_schematic` lane.

This is the FINAL V2.4 GROUNDING ENFORCEMENT PASS.

CURRENT STATE

The current V2 stack still has four unresolved truth problems:
1. `expected_family_grounded_coverage_rate` is too low
2. room/device evidence truth is not trustworthy enough
3. connector evidence truth is not trustworthy enough
4. hard-page fail counting truth is not strict enough

Your job is to fix those specifically.

PRIMARY GOAL

Make V2 genuinely earn its success by enforcing:
- family coverage
- room/device evidence truth
- connector evidence truth
- hard-page truth

DO NOT:
- touch OrbitBrief
- start V3
- add full raster fallback
- accept empty hard-page auto-pass
- accept default success booleans
- accept suspiciously uniform grounded rows
- stop after cosmetic metric improvements

IMPLEMENT IN THIS ORDER

## 1. Family coverage enforcement
Add explicit per-packet and per-hard-page family coverage computation:
- expected family set from schema
- grounded family set
- hard-page grounded family set
- family coverage rates

Add packet failure if:
- expected family grounded coverage is too low
- hard-page family grounded coverage is too low

## 2. Room/device evidence truth
`room_device_association_ok` must only be true when backed by:
- nearby room/device label
- same region/subregion/pseudo-page/detail frame
- leader attachment or local note/title context
- score crossing threshold

Add packet failure if:
- room/device association rate is implausibly high relative to score distribution
- room/device evidence sources are absent but the packet still reports high room association success

## 3. Connector evidence truth
`connector_grounding_ok` must only be true when backed by:
- connector candidates
- leader attachments
- riser/rack/pathway context
- connector score crossing threshold

Add packet failure if:
- connector quality is high while connector evidence is sparse or absent
- connector-heavy hard pages do not have strong connector-backed grounding

## 4. Hard-page fail counting truth
For each packet:
- derive required hard-page types from schema + actual page rows
- required hard-page set must not be empty when those sheet families exist
- packet hard-page semantics must fail when:
  - required pages are missing
  - grounded yield on hard pages is too low
  - family coverage on hard pages is too low
  - room/device or connector truth is weak on required pages

## 5. Sample-row audits
Emit packet-level sample rows containing raw evidence:
- legend_match_score
- legend_text_association_score
- room_device_association_score
- connector_context_score
- page_type_compatibility
- grounding_state
- connector_grounding_ok
- room_device_association_ok
- grounded_family

These must be saved in artifacts and used for auditing.

FILES MOST LIKELY TO CHANGE
- `src/orbitbrief_core/parser/site_schematic/models.py`
- `src/orbitbrief_core/parser/site_schematic/semantic_mapper.py`
- `src/orbitbrief_core/parser/site_schematic/grounding_resolver.py`
- `src/orbitbrief_core/parser/site_schematic/core.py`
- `tests/site_schematic/phase_v2_eval.py`

USE THE HELPER MODULES FROM THIS KIT
- `family_coverage_enforcement.py`
- `room_device_truth_audit.py`
- `connector_truth_audit.py`
- `hardpage_gate_enforcement.py`
- `sample_row_audit.py`

VALIDATION REQUIRED

Run:
- `pytest tests/site_schematic/test_phase_v2_helpers.py -q`
- `pytest tests/site_schematic/test_phase_v2_1_helpers.py -q`
- `pytest tests/site_schematic/test_phase_v2_2_helpers.py -q`
- new helper tests from this kit
- `python -m tests.site_schematic.phase_v2_eval`
- V0/V1 regression spot checks:
  - `pytest tests/site_schematic/test_phase_v0_v1_eval.py -q`
  - `pytest tests/site_schematic/test_phase_v0_v1_gap_closure_helpers.py -q`
- parser spot checks:
  - `pytest tests/site_schematic/test_gold_pdf_eval.py -q`
  - `pytest tests/site_schematic/test_phasec_region_fidelity.py -q`

TARGETS

Preserve:
- production_kpi_regression_count = 0
- contradiction_lane_separation_rate = 1.0
- V0/V1 stable
- parser text/table coverage stable

Must hit:
- expected_family_grounded_coverage_rate >= 0.75
- hardpage_family_grounded_coverage_rate >= 0.8
- room_device_evidence_truth_rate >= 0.9
- connector_evidence_truth_rate >= 0.9
- hardpage_requirement_truth_rate = 1.0
- hardpage_grounded_symbol_yield_rate >= 0.65
- grounding_state_honesty_rate >= 0.95
- packet_level_v2_failures = 0
- truth_audit_failures_total = 0

OUTPUT I WANT

1. exact files changed
2. what changed in family coverage enforcement
3. what changed in room/device truth
4. what changed in connector truth
5. what changed in hard-page gate enforcement
6. before/after V2 metrics
7. packet-by-packet weak packets, if any
8. before/after sample-row evidence quality
9. whether V2 is finally trustworthy enough for your two domains
