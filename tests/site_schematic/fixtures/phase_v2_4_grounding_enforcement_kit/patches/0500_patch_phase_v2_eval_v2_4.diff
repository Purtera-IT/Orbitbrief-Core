--- a/tests/site_schematic/phase_v2_eval.py
+++ b/tests/site_schematic/phase_v2_eval.py
@@
+from orbitbrief_core.parser.site_schematic.family_coverage_enforcement import compute_family_coverage
+from orbitbrief_core.parser.site_schematic.room_device_truth_audit import audit_room_device_truth
+from orbitbrief_core.parser.site_schematic.connector_truth_audit import audit_connector_truth
+from orbitbrief_core.parser.site_schematic.hardpage_gate_enforcement import enforce_hardpage_truth
+from orbitbrief_core.parser.site_schematic.sample_row_audit import select_grounding_sample_rows
@@
 def run_phase_v2_eval(...):
@@
+    family_cov_rates = []
+    hardpage_family_cov_rates = []
+    room_truth_hits = 0
+    connector_truth_hits = 0
+    hardpage_truth_hits = 0
+    room_truth_total = 0
+    connector_truth_total = 0
+    hardpage_truth_total = 0
@@
         metrics = compute_grounded_yield_metrics(
@@
         packet_row["grounded_symbol_yield_rate"] = metrics.grounded_symbol_yield_rate
         packet_row["hardpage_grounded_symbol_yield_rate"] = metrics.hardpage_grounded_symbol_yield_rate
         packet_row["unresolved_symbol_ratio"] = metrics.unresolved_symbol_ratio
         packet_row["expected_family_grounded_coverage_rate"] = metrics.expected_family_grounded_coverage_rate
+
+        expected_families = packet_row.get("expected_symbol_families", [])
+        grounded_rows = packet_row.get("grounded_symbol_rows", [])
+        grounded_families = [row.get("grounded_family", "") for row in grounded_rows if row.get("grounded_family")]
+        hardpage_grounded_rows = [row for row in grounded_rows if row.get("hardpage_page", False)]
+        hardpage_grounded_families = [row.get("grounded_family", "") for row in hardpage_grounded_rows if row.get("grounded_family")]
+        fam = compute_family_coverage(
+            expected_families=expected_families,
+            grounded_families=grounded_families,
+            hardpage_grounded_families=hardpage_grounded_families,
+        )
+        packet_row["expected_family_grounded_coverage_rate"] = fam.expected_family_grounded_coverage_rate
+        packet_row["hardpage_family_grounded_coverage_rate"] = fam.hardpage_family_grounded_coverage_rate
+        family_cov_rates.append(fam.expected_family_grounded_coverage_rate)
+        hardpage_family_cov_rates.append(fam.hardpage_family_grounded_coverage_rate)
+
+        room_scores = [float(row.get("room_device_association_score", 0.0) or 0.0) for row in grounded_rows]
+        room_truth = audit_room_device_truth(
+            association_rate=float(packet_row.get("room_device_association_rate", 0.0)),
+            room_assoc_scores=room_scores,
+            near_room_label_hits=sum(1 for row in grounded_rows if row.get("near_room_label", False)),
+            same_region_hits=sum(1 for row in grounded_rows if row.get("same_region", False)),
+            leader_attached_hits=sum(1 for row in grounded_rows if row.get("leader_attached", False)),
+        )
+        packet_row["room_device_evidence_truth_ok"] = room_truth.evidence_truth_ok and room_truth.score_distribution_ok
+        packet_row["room_device_truth_reasons"] = room_truth.reasons
+        room_truth_total += 1
+        if packet_row["room_device_evidence_truth_ok"]:
+            room_truth_hits += 1
+
+        connector_scores = [float(row.get("connector_context_score", 0.0) or 0.0) for row in grounded_rows]
+        connector_truth = audit_connector_truth(
+            connector_quality_rate=float(packet_row.get("connector_grounding_quality_rate", 0.0)),
+            connector_candidate_rate=float(packet_row.get("connector_topology_candidate_rate", 0.0)),
+            connector_scores=connector_scores,
+            leader_attachment_hits=sum(1 for row in grounded_rows if row.get("leader_attached", False)),
+        )
+        packet_row["connector_evidence_truth_ok"] = connector_truth.evidence_truth_ok
+        packet_row["connector_truth_reasons"] = connector_truth.reasons
+        connector_truth_total += 1
+        if packet_row["connector_evidence_truth_ok"]:
+            connector_truth_hits += 1
+
+        hardpage_gate = enforce_hardpage_truth(
+            required_page_types=packet_row.get("required_page_types", []),
+            satisfied_page_types=packet_row.get("satisfied_page_types", []),
+            hardpage_grounded_symbol_yield_rate=float(packet_row.get("hardpage_grounded_symbol_yield_rate", 0.0)),
+            hardpage_family_grounded_coverage_rate=fam.hardpage_family_grounded_coverage_rate,
+        )
+        packet_row["hardpage_requirement_truth_ok"] = hardpage_gate.hardpage_requirement_truth_ok
+        packet_row["hardpage_truth_reasons"] = hardpage_gate.reasons
+        hardpage_truth_total += 1
+        if packet_row["hardpage_requirement_truth_ok"]:
+            hardpage_truth_hits += 1
+
+        packet_row["grounding_sample_rows"] = select_grounding_sample_rows(grounded_rows, limit=15)
@@
+    summary["expected_family_grounded_coverage_rate"] = (sum(family_cov_rates) / len(family_cov_rates)) if family_cov_rates else 1.0
+    summary["hardpage_family_grounded_coverage_rate"] = (sum(hardpage_family_cov_rates) / len(hardpage_family_cov_rates)) if hardpage_family_cov_rates else 1.0
+    summary["room_device_evidence_truth_rate"] = (room_truth_hits / room_truth_total) if room_truth_total else 1.0
+    summary["connector_evidence_truth_rate"] = (connector_truth_hits / connector_truth_total) if connector_truth_total else 1.0
+    summary["hardpage_requirement_truth_rate"] = (hardpage_truth_hits / hardpage_truth_total) if hardpage_truth_total else 1.0
+
+    summary["truth_audit_failures_total"] = (
+        summary.get("empty_required_hardpage_packet_failures", 0)
+        + summary.get("suspicious_uniform_grounding_packet_failures", 0)
+        + summary.get("impossible_connector_success_packet_failures", 0)
+        + summary.get("impossible_room_assoc_packet_failures", 0)
+        + sum(1 for row in packet_rows if not row.get("room_device_evidence_truth_ok", False))
+        + sum(1 for row in packet_rows if not row.get("connector_evidence_truth_ok", False))
+        + sum(1 for row in packet_rows if not row.get("hardpage_requirement_truth_ok", False))
+    )
