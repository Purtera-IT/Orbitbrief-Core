You are working in the latest OrbitBrief Core repo on the `site_schematic` lane.

This is the FINAL TEXT-TAIL FIX PASS.

CURRENT STATE

The parser is already effectively complete for non-graphics content, but 4 page-level `text_only_gap` cases remain.

Those 4 pages are:

1. `lv_a_aspen_house_telecom_intercom_risers` page 59
   - `sheet_type = schedule_sheet`
   - `sheet_title = SCHEDULES & MISCELLANEOUS`
   - `table_count = 8`
   - `note_clause_count = 0`
   - reason: `note/spec cues present but no structured note clause emitted`

2. `tc_b_seele_es_refresh_dwgs` page 54
   - `sheet_type = legend_symbol`
   - `table_count = 2`
   - `note_clause_count = 0`
   - reason: `note/spec cues present but no structured note clause emitted`

3. `tc_b_seele_es_refresh_dwgs` page 99
   - `sheet_type = floorplan_overall`
   - `sheet_number = A300`
   - `table_count = 2`
   - `room_label_count = 5`
   - `note_clause_count = 0`
   - reason: `note/spec cues present but no structured note clause emitted`

4. `tc_b_seele_es_refresh_dwgs` page 100
   - `sheet_type = schedule_sheet`
   - `sheet_number = A300`
   - `table_count = 2`
   - `note_clause_count = 0`
   - reason: `note/spec cues present but no structured note clause emitted`

PRIMARY GOAL

Eliminate these last 4 parser-only text gaps so the parser is effectively perfect on non-graphics content.

DO NOT:
- work on OrbitBrief
- work on contradiction truth paths
- start graphics/schematic interpretation yet
- weaken eval criteria
- do a broad parser rewrite

WHAT TO IMPLEMENT

## 1. Add a residual note-gap registry
Create a deterministic registry for these exact final four pages with:
- packet_id
- page_index
- sheet_type
- promotion profile
- stricter/looser thresholds if needed
- notes on expected note-sidecar behavior

Use this registry only as the last bounded cleanup layer, not as a broad hack for everything.

## 2. Strengthen note promotion profiles
Implement or expand these profiles:
- `schedule_with_note_sidecar`
- `legend_with_note_sidecar`
- `floorplan_with_note_sidecar`

Rules:
- schedule/legend pages may promote note-like sidecar paragraphs even if not numbered, as long as they are clearly note/spec-like
- floorplan pages may only promote note clauses when explicit note cues are present and the block is not just a room/device label
- prefer long blocks, sidecar/header-adjacent blocks, and table-adjacent narratives
- do not promote plan labels, room names, or random annotations

## 3. Materialize promoted notes into final extractor outputs
Ensure promoted note candidates actually become structured note clause objects in the extractor output path for:
- schedule_sheet
- legend_symbol
- notes_spec
- riser_diagram
- floorplan_overall only when explicitly allowed by the residual profile / note cues

## 4. Keep strong global-note guard intact
Do not regress true page-global notes.
Do not over-localize notes.
Do not misclassify room labels as note clauses.

## 5. Re-run full corpus extraction and prove the last 4 pages are fixed
I want updated:
- `corpus_summary.json`
- per-document `gap_audit.json`
- per-document `notes_and_rules.json`
- validation summary

TARGETS

I want this pass to hit:
- remaining `text_only_gap` pages = 0
- `unresolved_text_only_gap_rate_avg <= 0.001`
- `note_spec_coverage_rate_avg >= 0.998`
- `legend_table_coverage_rate_avg = 1.0`
- `semantic_lineage_coverage_rate_avg = 1.0`
- `parser_ready_for_vision_handoff_rate = 1.0`

VALIDATION REQUIRED

Run:
- `pytest tests/site_schematic/test_text_perfection_helpers.py -q`
- `pytest tests/site_schematic/test_gold_pdf_eval.py -q`
- `pytest tests/site_schematic/test_universal_table_contract.py -q`
- `pytest tests/site_schematic/test_phasec_region_fidelity.py -q`
- `pytest tests/site_schematic/test_phase_d_universality.py -q`
- `pytest tests/site_schematic/test_parser_full_extraction_corpus.py -q`
- `python -m tests.site_schematic.parser_full_extraction_eval`

OUTPUT I WANT

1. exact files changed
2. the residual note-gap registry added
3. exact note-promotion rules changed
4. before/after corpus metrics
5. before/after status of the 4 target pages
6. whether parser-only perfection is now achieved for non-graphics
7. explicit statement on whether the next phase is now graphics/schematics
