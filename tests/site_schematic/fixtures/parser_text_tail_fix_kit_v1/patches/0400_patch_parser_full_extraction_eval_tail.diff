--- a/tests/site_schematic/parser_full_extraction_eval.py
+++ b/tests/site_schematic/parser_full_extraction_eval.py
@@
 # Add explicit report block for final 4 residual pages
+FINAL_TAIL_TARGETS = {
+    ("lv_a_aspen_house_telecom_intercom_risers", 59),
+    ("tc_b_seele_es_refresh_dwgs", 54),
+    ("tc_b_seele_es_refresh_dwgs", 99),
+    ("tc_b_seele_es_refresh_dwgs", 100),
+}
@@
 def summarize_corpus(...):
@@
+    tail_status = []
+    for rec in page_gap_records:
+        key = (rec.get("packet_id"), rec.get("page_index"))
+        if key in FINAL_TAIL_TARGETS:
+            tail_status.append({
+                "packet_id": rec.get("packet_id"),
+                "page_index": rec.get("page_index"),
+                "sheet_type": rec.get("sheet_type"),
+                "gap_classes": rec.get("gap_classes", []),
+                "reasons": rec.get("reasons", []),
+                "note_clause_count": rec.get("note_clause_count", 0),
+                "scoped_note_count": rec.get("scoped_note_count", 0),
+            })
+    corpus_summary["final_text_tail_status"] = tail_status
