--- a/src/orbitbrief_core/parser/site_schematic/extractors/__init__.py
+++ b/src/orbitbrief_core/parser/site_schematic/extractors/__init__.py
@@
 def extract_by_sheet_type(...):
@@
-    result = extractor(...)
+    result = extractor(...)
+    # Tail-fix safety: if promoted note candidates exist and output note clauses are still empty,
+    # downstream extractors should have another chance to materialize them.
+    try:
+        promoted_note_candidates = model_registry.get("text_perfection", {}).get("promoted_note_candidates", {}).get(page_index, [])
+        if promoted_note_candidates and not getattr(result, "note_clauses", None):
+            note_clauses = []
+            for cand in promoted_note_candidates:
+                note_clauses.append(
+                    build_note_clause_from_text(
+                        cand["text"],
+                        bbox=cand.get("bbox"),
+                        source_mode=cand.get("source_mode", "deterministic_note_promoter_v1"),
+                    )
+                )
+            result.note_clauses = note_clauses
+    except Exception:
+        pass
