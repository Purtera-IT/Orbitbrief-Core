--- a/tests/site_schematic/phase_v0_v1_eval.py
+++ b/tests/site_schematic/phase_v0_v1_eval.py
@@
 def run_phase_v0_v1_eval(...):
@@
+    suspicious_zero_page_failures = 0
+    packet_level_primitive_graph_failures = 0
+    leader_quality_pages = 0
+    leader_expected_pages = 0
+    dimension_quality_pages = 0
+    dimension_expected_pages = 0
+    dedup_rates = []
+    density_sanity_hits = 0
+    density_pages = 0
@@
+        for page_row in rows.get("page_rows", []):
+            if page_row.get("suspicious_zero_primitive", False):
+                suspicious_zero_page_failures += 1
+            if "dedup_effectiveness" in page_row:
+                dedup_rates.append(float(page_row["dedup_effectiveness"]))
+            if "density_sanity_ok" in page_row:
+                density_pages += 1
+                density_sanity_hits += 1 if page_row["density_sanity_ok"] else 0
+            if page_row.get("leader_expected", False):
+                leader_expected_pages += 1
+                if page_row.get("leader_semantic_quality_ok", False):
+                    leader_quality_pages += 1
+            if page_row.get("dimension_expected", False):
+                dimension_expected_pages += 1
+                if page_row.get("dimension_semantic_quality_ok", False):
+                    dimension_quality_pages += 1
@@
+        if any(page_row.get("suspicious_zero_primitive", False) for page_row in rows.get("page_rows", [])):
+            packet_level_primitive_graph_failures += 1
@@
+    summary_json["suspicious_zero_primitive_page_failures"] = suspicious_zero_page_failures
+    summary_json["suspicious_zero_primitive_packet_failures"] = packet_level_primitive_graph_failures
+    summary_json["primitive_dedup_effectiveness_rate"] = (sum(dedup_rates) / len(dedup_rates)) if dedup_rates else 1.0
+    summary_json["primitive_density_sanity_rate"] = (density_sanity_hits / density_pages) if density_pages else 1.0
+    summary_json["leader_semantic_quality_rate"] = (leader_quality_pages / leader_expected_pages) if leader_expected_pages else 1.0
+    summary_json["dimension_semantic_quality_rate"] = (dimension_quality_pages / dimension_expected_pages) if dimension_expected_pages else 1.0
