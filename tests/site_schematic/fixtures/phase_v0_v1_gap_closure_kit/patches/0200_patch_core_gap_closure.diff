--- a/src/orbitbrief_core/parser/site_schematic/core.py
+++ b/src/orbitbrief_core/parser/site_schematic/core.py
@@
 from orbitbrief_core.parser.site_schematic.modality_calibration import calibrate_modality_decision
 from orbitbrief_core.parser.site_schematic.packet_v0_v1_quality import summarize_packet_v0_v1
+from orbitbrief_core.parser.site_schematic.modality_zero_guard import detect_suspicious_zero_primitive_page
@@
     model_registry.setdefault("phase_v0_v1", {})["page_modality"] = [
@@
     ]
+    # Page-level suspicious zero-primitive flags
+    page_zero_flags = []
+    for idx, obs in enumerate(page_observations):
+        decision = phase_v0_modality[idx]
+        raw_count = len(getattr(obs, "vector_primitives", []) or [])
+        validated_count = len(getattr(obs, "validated_vector_primitives", []) or [])
+        flag = detect_suspicious_zero_primitive_page(
+            modality=decision.modality,
+            vector_path_count=int(getattr(obs, "vector_path_count", 0) or 0),
+            image_count=int(getattr(obs, "image_count", 0) or 0),
+            line_art_density=float(getattr(obs, "line_art_density", 0.0) or 0.0),
+            primitive_count=raw_count,
+            validated_primitive_count=validated_count,
+        )
+        page_zero_flags.append({
+            "page_index": idx,
+            "suspicious": flag.suspicious,
+            "severity": flag.severity,
+            "reasons": flag.reasons,
+        })
+    model_registry["phase_v0_v1"]["suspicious_zero_primitive_pages"] = page_zero_flags
