You are working in the latest OrbitBrief Core repo on the `site_schematic` lane.

This is the V0/V1 GAP-CLOSURE PASS.

CURRENT STATE

V0/V1 already look strong, but three trust gaps remain:
1. one suspicious zero-primitive packet slipping through
2. primitive density / dedup quality not yet audited enough
3. leader/dimension metrics are presence metrics, not semantic quality metrics

PRIMARY GOAL

Close those three gaps and turn V0/V1 into a truly trustworthy base for V2.

DO NOT:
- touch OrbitBrief
- start V2
- add full raster fallback
- weaken parser truth paths
- regress current-pair parser behavior

IMPLEMENT IN THIS ORDER

## 1. Suspicious zero-primitive guard
Add page-level and packet-level logic so that:
- vector_rich pages with zero meaningful primitives are flagged
- hybrid pages with high vector evidence and zero meaningful primitives are flagged
- repeated suspicious pages trigger packet-level primitive graph failure

## 2. Primitive dedup / fusion
Add deterministic dedup before graph assembly:
- group near-duplicate lines / polylines / boxes
- preserve provenance counts
- export raw_count, deduped_count, validated_count, dedup_ratio

## 3. Primitive density audit
Add page-level and packet-level metrics:
- raw primitive density
- deduped primitive density
- validated primitive density
- isolated primitive ratio
- sparse graph flag
- overly dense graph flag

## 4. Leader semantic-quality scoring
Replace simple presence-only leader metrics with stronger quality scoring:
- long enough
- plausible aspect ratio
- terminates near text/symbol candidate when possible
- not just random short lines

## 5. Dimension semantic-quality scoring
Replace simple presence-only dimension metrics with stronger quality scoring:
- plausible dimension-line geometry
- nearby numeric / measurement text if available
- witness-line-like support if available
- not just arbitrary long linework

## 6. Packet-level V0/V1 summaries
Produce packet rows with:
- suspicious zero-primitive page count
- primitive_dedup_effectiveness
- primitive_density_sanity
- leader_semantic_quality
- dimension_semantic_quality
- packet pass/fail

FILES MOST LIKELY TO CHANGE
- `src/orbitbrief_core/parser/site_schematic/models.py`
- `src/orbitbrief_core/parser/site_schematic/core.py`
- `src/orbitbrief_core/parser/site_schematic/observations.py`
- `src/orbitbrief_core/parser/site_schematic/vector_primitives.py`
- `src/orbitbrief_core/parser/site_schematic/vector_primitive_graph.py`
- `tests/site_schematic/phase_v0_v1_eval.py`

USE THE HELPER MODULES FROM THIS KIT
- `modality_zero_guard.py`
- `primitive_dedup.py`
- `primitive_density_audit.py`
- `leader_dimension_quality.py`

VALIDATION REQUIRED

Run:
- `pytest tests/site_schematic/test_phase_v0_v1_helpers.py -q`
- `pytest tests/site_schematic/test_phase_v0_v1_perfection_helpers.py -q`
- new helper tests from this kit
- full 12-packet V0/V1 eval
- parser regression spot checks:
  - `test_gold_pdf_eval.py`
  - `test_universal_table_contract.py`
  - `test_phasec_region_fidelity.py`

TARGETS

Preserve:
- production_kpi_regression_count = 0
- contradiction_lane_separation_rate = 1.0
- parser text/table coverage unchanged

Close gaps:
- suspicious_zero_primitive_packet_failures = 0
- suspicious_zero_primitive_page_failures = 0
- primitive_dedup_effectiveness_rate >= 0.9
- primitive_density_sanity_rate >= 0.95
- leader_semantic_quality_rate >= 0.9
- dimension_semantic_quality_rate >= 0.85
- packet_level_primitive_graph_failures = 0

OUTPUT I WANT

1. exact files changed
2. what changed for zero-primitive sanity
3. what changed for dedup/fusion
4. what changed for density auditing
5. what changed for leader semantic quality
6. what changed for dimension semantic quality
7. before/after V0/V1 metrics
8. packet-by-packet weak pages, if any
9. whether V0/V1 are now truly locked for V2
