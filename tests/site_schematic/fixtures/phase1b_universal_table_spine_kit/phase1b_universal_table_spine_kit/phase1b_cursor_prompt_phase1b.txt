You are working in the latest OrbitBrief Core repo on the `site_schematic` lane.

This is PHASE 1B of the parser-only Universal Lossless Table Spine work.

CURRENT STATE

Phase 1 landed the universal table contract successfully, but the hard-page gold eval is still `not_perfect`.

Current result pattern:
- bbox/provenance are good when tables are found
- lineage completeness is structurally good when references exist
- BUT required table kind coverage is only 0.6
- semantic row/cell reference coverage is still very low
- hardest misses are on hybrid control/legend/detail pages

MAIN PROBLEM TO FIX

The parser is preserving tables when it finds them, but it is:
1. under-splitting hybrid pages into separate table candidates
2. under-classifying table families on hard pages
3. still letting downstream semantic extractors use lexical/regex fallbacks too often instead of table-row lineage

MISSION

Make the Phase-1 table spine actually approach perfection by implementing:

1. header-aware multi-table splitting
2. stronger table-kind routing
3. embedded-table promotion on detail pages
4. mandatory table-first routing for table-derived semantic extractors

DO NOT WORK ON ORBITBRIEF.
DO NOT DO A BROAD REGION-FIDELITY REWRITE YET.
KEEP THIS PARSER-ONLY.

WHAT TO IMPLEMENT

A. HEADER-AWARE MULTI-TABLE SPLITTING

Before table-kind inference, split hybrid pages into distinct table candidates using:
- boxed headers
- section header text
- local grid boundaries
- vertical separator logic
- local geometry / block grouping

Target pages:
- wireless `TC001`
- Southern Post `T001`
- mixed/embedded detail pages like T900/T905

B. STRONGER TABLE-KIND ROUTER

For each table candidate, infer kind using:
- header text
- column signatures
- row token patterns
- family-specific cue words

Kinds must include at least:
- drawing_index
- symbol_legend
- abbreviation_matrix / generic_grid equivalent
- outlet_definition
- schedule
- component_spec
- manufacturer_part_table
- embedded_detail_schedule
- responsibility_matrix

C. EMBEDDED TABLE PROMOTION

On detail pages, detect and promote small boxed schedule-like regions into table candidates when:
- local header exists
- row alignment exists
- table geometry is plausible
- this is better than flattening into notes/detail text

D. TABLE-FIRST SEMANTIC ROUTING

Update table-derived extractors so they prefer universal table rows/cells first and only fall back to regex/text if no valid table of expected kind exists.

This applies to:
- drawing index extraction
- legend extraction
- abbreviation extraction
- outlet type extraction
- schedule/spec row extraction

Every semantic object produced from a table must carry:
- source_table_id
- source_row_id
- source_cell_ids

TARGET HARD-PAGE GOLD CRITERIA

Make these pages satisfy the contract:

1. wireless_tc001_page1
Must detect:
- drawing_index
- symbol_legend
- outlet_definition
- generic_grid or equivalent abbreviation matrix

2. southern_t001_page2
Must detect:
- dense legend tables cleanly
- generic_grid / responsibility matrix coverage
- no collapse into one vague legend blob

3. southern_t900_page12
Must detect:
- embedded_detail_schedule where applicable

And preserve current good behavior on:
- southern_t000_page1
- southern_t002_page3
- southern_t905_page17

PERFECT PHASE-1B TARGET

I want the hard-page gold eval to reach as close as possible to:
- required_table_kind_coverage = 1.0
- bbox_presence_rate = 1.0
- lineage_completeness_rate = 1.0
- semantic_row_reference_rate >= 0.95
- semantic_cell_reference_rate >= 0.95
- unflagged row/cell merge/split counts = 0

VALIDATION REQUIRED

Run and report:
- the hard-page universal table contract eval
- test_universal_table_contract.py
- test_mixed_detail_decomposition.py
- test_subregion_dispatch.py
- test_note_scope_resolution.py
- test_graph_subregion_edges.py
- test_observation_policy_hardening.py
- test_model_assisted_observations.py
- test_phase2_pdf_smoke.py
- test_gold_pdf_eval.py

OUTPUT I WANT

When done, give me:
1. exact files changed
2. what header-aware splitting rules were added
3. what table-kind routing rules were added
4. what embedded-table promotion rules were added
5. which extractors now use table-first routing
6. before/after hard-page gold metrics
7. page-by-page misses that remain, if any
8. whether Phase-1 “perfect table spine” is now satisfied
