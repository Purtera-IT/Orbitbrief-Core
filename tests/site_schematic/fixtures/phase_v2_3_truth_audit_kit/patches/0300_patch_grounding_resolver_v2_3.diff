--- a/src/orbitbrief_core/parser/site_schematic/grounding_resolver.py
+++ b/src/orbitbrief_core/parser/site_schematic/grounding_resolver.py
@@
+from orbitbrief_core.parser.site_schematic.evidence_backed_flags import evidence_backed_connector_ok, evidence_backed_grounded_ok
@@
 def resolve_grounded_symbols(...):
@@
+    connector_ok = evidence_backed_connector_ok(
+        connector_context_score=resolution_metadata.get("connector_context_score", 0.0),
+        connector_candidate_count=connector_candidate_count if 'connector_candidate_count' in locals() else 0,
+        leader_attachment_count=leader_attachment_count if 'leader_attachment_count' in locals() else 0,
+    )
+    grounded_ok = evidence_backed_grounded_ok(
+        grounding_state=resolution_metadata.get("grounding_state", "unresolved"),
+        legend_match_score=legend_match_score if 'legend_match_score' in locals() else 0.0,
+        legend_text_association_score=legend_text_score if 'legend_text_score' in locals() else 0.0,
+        connector_context_score=resolution_metadata.get("connector_context_score", 0.0),
+        room_device_association_score=resolution_metadata.get("room_device_association_score", 0.0),
+        page_type_compatibility=page_type_compatibility if 'page_type_compatibility' in locals() else 0.0,
+    )
+    if resolution_metadata.get("grounding_state") == "grounded" and not grounded_ok:
+        resolution_metadata["grounding_state"] = "ambiguous"
+        resolution_metadata.setdefault("grounding_state_reasons", []).append("downgraded_by_truth_audit")
+    resolution_metadata["connector_grounding_ok"] = connector_ok
