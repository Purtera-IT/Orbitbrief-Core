# Tests known to fail on main as of 2026-08-20.
#
# This file is a ratchet, not an amnesty. CI deselects everything listed here
# so that a NEW failure turns the build red immediately, and separately re-runs
# this list and reports any entry that now passes. When CI says an entry
# passes, delete the line -- the list is only allowed to shrink.
#
# 5 entries, down from 12 on the first honest run of this suite. The other
# seven were two real bugs, fixed in the same PR that created this file.

# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Behavioural — product decisions, not bugs (4)
#
# Each asserts something about what a PM sees. Changing the code to satisfy
# them changes the product, so they want a human deciding which side is right.
#
#   hides_internal_language      'physical_site' reaches PM-facing markdown
#   polish_items_batches         batches 4 where the test expects 3
#   av_drops_speculative         a speculative risk blocker is not dropped
#   coverage_specializes         a question that should be suppressed is not
# ---------------------------------------------------------------------------
tests/pm_handoff/test_pm_handoff_builder.py::test_pm_handoff_hides_internal_language
tests/pm_handoff/test_polish.py::test_polish_items_batches_requests
tests/pm_handoff/test_question_engine.py::test_av_drops_speculative_carpet_ceiling_risk_blockers
tests/pm_handoff/test_question_quality_sharp.py::test_coverage_specializes_or_suppresses
