"""Phase-1 fixtures live in the root ``tests/conftest.py`` so Phase 2 can reuse them.

This file remains as an explicit marker that Phase-1 tests depend
on those fixtures (mixed_envelope, mixed_artifact_dir, etc.).
Adding a phase-1-only fixture? Put it here.
"""
